# YCDesk 连接测试和调试指南

## 快速测试步骤

### 1. 启动信令服务器

```bash
cd server
npm install  # 如果还没安装依赖
node server.js
```

你应该看到：
```
YCDesk 信令服务器运行在端口 3000
WebSocket 服务器已启动
```

### 2. 启动 Windows 客户端

```bash
cd ..  # 回到项目根目录
npm start
```

### 3. 启动第二个 Windows 客户端（测试用）

打开新的终端窗口：
```bash
cd d:\MyProg\YCDesk
npm start
```

### 4. 连接测试

**在客户端1（主控端）：**
1. 确认连接模式是"信令服务器模式"
2. 信令服务器地址应该是：`http://localhost:3000`
3. 点击连接（应该显示"已连接"）
4. 复制客户端1的设备ID（例如：ABC123DEF）

**在客户端2（被控端）：**
1. 同样连接到信令服务器 `http://localhost:3000`
2. 复制客户端2的设备ID

**连接两台设备：**
1. 在客户端1输入客户端2的设备ID
2. 点击"连接"
3. 客户端2应该弹出连接确认对话框
4. 点击"接受"
5. 应该开始建立WebRTC连接

---

## 常见问题诊断

### 问题1：无法连接到信令服务器

**症状：** 显示"连接失败"或"连接超时"

**检查清单：**
- [ ] 信令服务器是否正在运行？
- [ ] 服务器地址是否正确？（Windows本地测试用 `http://localhost:3000`）
- [ ] 防火墙是否阻止了连接？
- [ ] 控制台是否有错误信息？

**测试服务器连接：**
在浏览器打开：`http://localhost:3000`
如果无法打开，说明服务器没启动或端口被占用。

### 问题2：信令服务器已连接，但无法找到对方设备

**症状：** 显示"设备不在线或不存在"

**检查清单：**
- [ ] 两台设备是否都连接到了同一个信令服务器？
- [ ] 两台设备的服务器地址是否完全一致？
- [ ] 设备ID是否输入正确（9位字符，大小写不敏感）？
- [ ] 信令服务器控制台是否显示两台设备都已注册？

**查看服务器日志：**
信令服务器控制台应该显示：
```
设备连接: <socket-id>
设备注册: <设备ID>
```

### 问题3：连接请求已发送，但对方没有收到

**症状：** 点击连接后，对方没有弹出确认框

**检查清单：**
- [ ] 信令服务器控制台是否显示"连接请求"日志？
- [ ] 两台设备的设备ID是否弄反了？
- [ ] 接收方的页面是否在前台？

### 问题4：对方已接受，但WebRTC连接失败

**症状：** 显示"连接失败"或长时间"连接中"

**可能原因：**
- STUN服务器无法访问
- 网络环境不支持P2P
- 防火墙阻止了WebRTC流量

**解决方案：**
1. 检查网络连接
2. 尝试更换网络环境
3. 配置TURN服务器（高级）

---

## 获取调试日志

### Windows客户端调试

1. 在Windows客户端按 `F12` 打开开发者工具
2. 切换到 `Console` 标签
3. 查看日志输出

### 信令服务器日志

信令服务器控制台会显示所有连接和消息日志。

---

## 局域网内连接（多台电脑）

如果你有两台电脑在同一局域网：

### 步骤1：获取服务器电脑的IP

在服务器电脑上：
```bash
ipconfig
```
找到"IPv4 地址"，例如：`192.168.1.100`

### 步骤2：修改客户端连接地址

在所有客户端上，将信令服务器地址改为：
```
http://192.168.1.100:3000
```
（用实际的IP地址替换）

### 步骤3：确保防火墙允许

在服务器电脑上，允许端口3000通过防火墙。

---

## 使用Android客户端连接

### 前提条件

- Android设备和Windows电脑在同一局域网
- 或者信令服务器部署在公网

### 步骤

1. **启动信令服务器**（在Windows电脑上）
2. **获取Windows电脑的局域网IP**
   ```bash
   ipconfig
   ```
3. **在Android应用中输入服务器地址**
   ```
   http://192.168.1.100:3000
   ```
   （替换为实际IP）
4. **连接测试**

---

## 快速测试脚本

### 测试信令服务器

创建 `test-server.js`：
```javascript
const io = require('socket.io-client');

const socket = io('http://localhost:3000');

socket.on('connect', () => {
  console.log('✅ 已连接到信令服务器');
  socket.emit('register', 'TEST001');
});

socket.on('disconnect', () => {
  console.log('❌ 已断开连接');
});

socket.on('connect_error', (err) => {
  console.log('❌ 连接错误:', err);
});
```

运行测试：
```bash
node test-server.js
```

---

## 成功连接的标志

### 信令服务器控制台
```
设备连接: <socket-id-1>
设备注册: <设备ID-1>
设备连接: <socket-id-2>
设备注册: <设备ID-2>
连接请求: <设备ID-1> -> <设备ID-2>
连接建立: <session-id>
```

### 客户端控制台
```
已连接到信令服务器
收到连接请求: { fromDeviceId: '...' }
作为主控端建立连接
收到远程媒体流
连接状态: connected
```

---

## 仍然有问题？

请提供以下信息：
1. 信令服务器控制台的完整日志
2. 两个客户端的控制台错误信息
3. 你使用的连接方式（本地/局域网/公网）
4. 具体卡在哪个步骤
