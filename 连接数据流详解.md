# YCDesk 连接数据流详解

## 一、两种连接模式概述

YCDesk 支持两种连接模式：

1. **信令服务器模式** - 通过中间服务器交换连接参数，适合大多数网络环境
2. **直连模式** - 直接交换连接参数，适合两台设备有固定公网IP的场景

---

## 二、信令服务器模式数据流

### 2.1 架构图

```
┌─────────────┐         ┌─────────────────┐         ┌─────────────┐
│  主控端     │◄───────►│  信令服务器     │◄───────►│  被控端     │
│ (Controller)│         │  (Signaling)    │         │ (Controlled)│
└─────────────┘         └─────────────────┘         └─────────────┘
       │                                              │
       │                                              │
       └────────────────── WebRTC P2P ──────────────┘
                    (媒体流直接传输)
```

### 2.2 详细数据流步骤

#### 阶段1：设备注册

1. **被控端启动**
   - 生成唯一设备ID（9位随机字符）
   - 连接到信令服务器：`socket = io(serverUrl)`
   - Socket.IO建立WebSocket连接
   - 发送注册消息：`socket.emit('register', deviceId)`
   - 信令服务器记录：`devices.set(deviceId, { socketId, online: true })`

2. **主控端启动**
   - 同样生成设备ID并连接信令服务器
   - 注册到信令服务器

#### 阶段2：发起连接请求

1. **主控端用户操作**
   - 输入被控端的设备ID
   - 点击"连接"按钮

2. **发送连接请求**
   ```javascript
   socket.emit('connect-request', {
     fromDeviceId: 主控端ID,
     toDeviceId: 被控端ID
   });
   ```

3. **信令服务器转发**
   - 查找被控端的socketId
   - 转发请求：`io.to(targetSocketId).emit('incoming-connection', data)`
   - 生成sessionId（会话ID）

#### 阶段3：接受/拒绝连接

1. **被控端收到请求**
   - 显示确认对话框："设备XXX想要连接，是否接受？"

2. **用户选择接受**
   - 发送响应：
   ```javascript
   socket.emit('connection-response', {
     sessionId: sessionId,
     accepted: true,
     fromDeviceId: 主控端ID,
     toDeviceId: 被控端ID
   });
   ```

3. **信令服务器转发响应**
   - 转发给主控端：`io.to(fromSocketId).emit('connection-result', data)`

#### 阶段4：WebRTC连接建立

**主控端（发起方）：**

1. 创建PeerConnection
   ```javascript
   peerConnection = new RTCPeerConnection({
     iceServers: [
       { urls: 'stun:stun.l.google.com:19302' }
     ]
   });
   ```

2. 创建数据通道
   ```javascript
   dataChannel = peerConnection.createDataChannel('control');
   ```

3. 生成Offer
   ```javascript
   const offer = await peerConnection.createOffer();
   await peerConnection.setLocalDescription(offer);
   ```

4. 通过信令服务器发送Offer
   ```javascript
   socket.emit('offer', {
     sessionId: sessionId,
     offer: offer,
     toDeviceId: 被控端ID
   });
   ```

**被控端（接收方）：**

1. 收到Offer
   ```javascript
   await peerConnection.setRemoteDescription(new RTCSessionDescription(offer));
   ```

2. 生成Answer
   ```javascript
   const answer = await peerConnection.createAnswer();
   await peerConnection.setLocalDescription(answer);
   ```

3. 发送Answer
   ```javascript
   socket.emit('answer', {
     sessionId: sessionId,
     answer: answer,
     toDeviceId: 主控端ID
   });
   ```

**主控端：**

1. 收到Answer
   ```javascript
   await peerConnection.setRemoteDescription(new RTCSessionDescription(answer));
   ```

#### 阶段5：ICE候选交换

1. **双方收集ICE候选**
   - WebRTC自动收集网络候选（内网IP、外网IP、中继地址等）
   - 每次收集到候选触发`onicecandidate`事件

2. **通过信令服务器交换候选**
   ```javascript
   socket.emit('ice-candidate', {
     sessionId: sessionId,
     candidate: candidate,
     toDeviceId: 对方ID
   });
   ```

3. **添加对方的候选**
   ```javascript
   await peerConnection.addIceCandidate(new RTCIceCandidate(candidate));
   ```

#### 阶段6：媒体流传输

**被控端：**

1. 捕获屏幕
   ```javascript
   const stream = await navigator.mediaDevices.getUserMedia({
     video: {
       mandatory: {
         chromeMediaSource: 'desktop',
         chromeMediaSourceId: sourceId
       }
     }
   });
   ```

2. 添加媒体流到PeerConnection
   ```javascript
   stream.getTracks().forEach(track => {
     peerConnection.addTrack(track, stream);
   });
   ```

**主控端：**

1. 收到媒体流
   ```javascript
   peerConnection.ontrack = (event) => {
     const stream = event.streams[0];
     videoElement.srcObject = stream;
   };
   ```

#### 阶段7：数据通道传输

**主控端发送输入事件：**

```javascript
dataChannel.send(JSON.stringify({
  type: 'input',
  inputType: 'mousemove',
  x: 0.5,  // 归一化坐标
  y: 0.3
}));
```

**被控端接收并执行：**

```javascript
dataChannel.onmessage = (event) => {
  const data = JSON.parse(event.data);
  if (data.type === 'input') {
    // 使用Electron Input API模拟鼠标/键盘事件
    window.electronAPI.send('remote-input', data);
  }
};
```

---

## 三、直连模式数据流

### 3.1 架构图

```
┌─────────────┐                                    ┌─────────────┐
│  设备A      │                                    │  设备B      │
│ (主控端)    │                                    │ (被控端)    │
└─────────────┘                                    └─────────────┘
       │                                                  │
       │  1. 生成Offer                                    │
       ├─────────────────────────────────────────────────►│
       │              (手动复制粘贴)                       │
       │                                                  │
       │  2. 生成Answer                                   │
       │◄─────────────────────────────────────────────────┤
       │              (手动复制粘贴)                       │
       │                                                  │
       │                                                  │
       └────────────────── WebRTC P2P ───────────────────┘
                    (媒体流直接传输)
```

### 3.2 详细数据流步骤

#### 阶段1：主控端发起连接

1. **用户选择直连模式**
   - 切换到"直连模式"
   - （可选）输入对方IP地址（仅用于记录）

2. **点击"连接"按钮**
   ```javascript
   async function startDirectConnection() {
     // 创建PeerConnection，不使用STUN/TURN服务器
     directPeerConnection = new RTCPeerConnection({ iceServers: [] });
     
     // 创建数据通道
     dataChannel = directPeerConnection.createDataChannel('direct-control');
     
     // 生成Offer
     const offer = await directPeerConnection.createOffer();
     await directPeerConnection.setLocalDescription(offer);
     
     // 编码Offer（Base64）方便传输
     const offerText = btoa(JSON.stringify(offer));
     
     // 复制到剪贴板
     await navigator.clipboard.writeText(offerText);
   }
   ```

3. **用户手动发送Offer**
   - 通过QQ、微信、邮件等方式将Offer发送给对方

#### 阶段2：被控端接受连接

1. **用户切换到直连模式**
   - 粘贴收到的Offer到输入框

2. **点击"接受连接"按钮**
   ```javascript
   async function acceptDirectConnection() {
     const offerInput = document.getElementById('offerInput').value;
     
     // 解码Offer
     const offer = JSON.parse(atob(offerInput));
     
     // 创建PeerConnection
     directPeerConnection = new RTCPeerConnection({ iceServers: [] });
     
     // 设置远程描述
     await directPeerConnection.setRemoteDescription(
       new RTCSessionDescription(offer)
     );
     
     // 生成Answer
     const answer = await directPeerConnection.createAnswer();
     await directPeerConnection.setLocalDescription(answer);
     
     // 编码Answer
     const answerText = btoa(JSON.stringify(answer));
     
     // 复制到剪贴板
     await navigator.clipboard.writeText(answerText);
     
     // 开始捕获屏幕
     await startScreenCapture();
   }
   ```

3. **用户手动发送Answer**
   - 将Answer发回给主控端

#### 阶段3：主控端完成连接

1. **粘贴Answer**
   - 在主控端输入框粘贴收到的Answer
   - （注：当前实现自动完成此步骤，通过confirm对话框引导）

2. **设置远程描述**
   ```javascript
   await peerConnection.setRemoteDescription(
     new RTCSessionDescription(answer)
   );
   ```

#### 阶段4：ICE候选和媒体流

与信令服务器模式相同，但：
- **不使用STUN/TURN服务器**（因为假设双方有公网IP）
- **ICE候选仍然会交换**，但只包含公网IP候选
- **媒体流直接点对点传输**

---

## 四、关键技术点详解

### 4.1 Socket.IO vs WebSocket

**Socket.IO**（信令服务器使用）：
- 自动降级：WebSocket → HTTP长轮询 → HTTP轮询
- 自动重连
- 房间/命名空间支持
- 消息确认机制

**为什么用Socket.IO做信令？**
- 信令消息量小，不需要极致性能
- 需要保证可靠性（连接参数不能丢）
- 需要处理各种网络环境

### 4.2 WebRTC关键概念

#### SDP (Session Description Protocol)
- **Offer**: 发起方的会话描述（支持的编码、媒体格式等）
- **Answer**: 应答方的会话描述
- 包含：媒体类型、编解码器、网络地址等

#### ICE (Interactive Connectivity Establishment)
- **STUN服务器**: 获取公网IP地址
- **TURN服务器**: 中继媒体流（穿透失败时使用）
- **ICE候选**: 所有可能的连接地址组合

#### Candidate Types
- **Host**: 本地内网IP
- **Server Reflexive**: NAT映射的公网IP（通过STUN获取）
- **Relayed**: TURN服务器的中继地址

### 4.3 数据通道 vs 媒体通道

| 特性 | 媒体通道 | 数据通道 |
|------|---------|---------|
| 用途 | 视频/音频 | 控制命令、文件传输 |
| 协议 | SRTP (加密) | SCTP (可靠传输) |
| 延迟 | 低（可接受丢包） | 可配置 |
| 可靠性 | 不可靠（UDP-like） | 可靠（TCP-like） |

### 4.4 NAT穿透原理

```
内网设备 (192.168.1.100)
      │
      │ 1. 发送STUN请求
      ▼
   NAT路由器
      │
      │ 2. 分配公网端口 (203.0.113.10:54321)
      ▼
   STUN服务器
      │
      │ 3. 返回公网地址
      ▼
内网设备 (获得自己的公网地址)
```

---

## 五、对比市面商业化软件

### 5.1 ToDesk/TeamViewer架构

```
┌─────────┐         ┌──────────┐         ┌─────────┐
│ 客户端A  │◄───────►│ 中继服务器 │◄───────►│ 客户端B  │
└─────────┘         └──────────┘         └─────────┘
       │                    │                    │
       └───────── P2P（优先）───────────────────┘
              (失败时走中继)
```

**关键差异：**
1. **混合模式**: 优先P2P，失败自动走中继
2. **专有协议**: 不使用标准WebRTC，自己实现UDP传输
3. **全局服务器**: 部署大量全球中继服务器
4. **智能路由**: 自动选择最优连接路径

### 5.2 为什么YCDesk使用WebRTC？

| 特性 | WebRTC | 自研协议 |
|------|--------|---------|
| 开发速度 | 快 | 慢 |
| 跨平台 | 优秀 | 需要适配 |
| 浏览器支持 | 原生 | 不支持 |
| 标准性 | 开放标准 | 封闭 |
| 可控性 | 中等 | 高 |
| NAT穿透 | 一般（依赖STUN/TURN） | 优秀（专有技术） |

### 5.3 YCDesk的改进空间

1. **部署STUN/TURN服务器** - 提高穿透成功率
2. **实现中继功能** - P2P失败时备用
3. **优化ICE候选收集** - 更快建立连接
4. **添加心跳机制** - 检测连接状态
5. **实现断线重连** - 自动恢复连接

---

## 六、常见问题排查

### 6.1 连接失败常见原因

| 现象 | 可能原因 | 解决方案 |
|------|---------|---------|
| 无法连接信令服务器 | 服务器未启动 | 检查server/server.js是否运行 |
| | 防火墙拦截 | 开放3000端口 |
| | 地址错误 | 检查serverUrl配置 |
| WebRTC连接失败 | 内网无法穿透 | 部署TURN服务器 |
| | 防火墙阻止 | 开放UDP端口1024-65535 |
| | STUN服务器不可用 | 更换STUN服务器 |
| 直连模式失败 | 无公网IP | 使用信令服务器模式 |
| | NAT对称型 | 无法直连，必须用中继 |
| | Offer/Answer未正确交换 | 检查是否完整复制粘贴 |

### 6.2 日志检查

**信令服务器日志：**
```
设备连接: xxx
设备注册: DEVICE123
连接请求: DEVICE123 -> DEVICE456
连接建立: sess_xxx
```

**客户端日志（浏览器控制台）：**
```
[时间] YCDesk 初始化完成，设备ID: XXX
[时间] 正在连接信令服务器: http://...
[时间] ✓ 已连接到信令服务器，Socket ID: ...
[时间] 正在注册设备 ID: ...
```

---

## 七、总结

YCDesk的连接架构：

1. **信令服务器模式** - 推荐，适合99%场景
   - 简单易用
   - 自动NAT穿透
   - 需要一台中间服务器

2. **直连模式** - 特殊场景
   - 延迟最低
   - 需要公网IP
   - 手动交换参数

两种模式的核心都是WebRTC，区别仅在于信令交换方式不同！
