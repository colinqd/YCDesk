{
  "name": "ycdesk-android",
  "version": "0.2.1",
  "description": "YCDesk - Android版本（包含信令服务器）",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo \"No tests configured for android. Run 'npm test' from windows/ to run shared tests.\" && exit 0",
    "cap:init": "cap init",
    "cap:add": "cap add android",
    "cap:sync": "cap sync",
    "cap:open": "cap open android",
    "cap:build": "npm run build && cap sync && cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/device": "^6.0.0",
    "@capacitor/network": "^6.0.0",
    "socket.io-client": "^4.6.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "vite": "^5.4.21"
  }
}
