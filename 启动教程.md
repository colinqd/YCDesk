# YCDesk Windows 启动教程

## 第一步：安装 Node.js

如果还没有安装 Node.js，请先下载安装：
https://nodejs.org/ (推荐 LTS 版本)

安装完成后，打开 PowerShell 或 CMD 检查：
```
node --version
npm --version
```

## 第二步：安装项目依赖

### 方式一：使用命令行（推荐）

1. 打开 PowerShell 或 CMD
2. 进入项目目录：
   ```
   cd d:\MyProg\YCDesk
   ```

3. 安装客户端依赖：
   ```
   npm install
   ```

4. 安装服务器依赖：
   ```
   cd server
   npm install
   cd ..
   ```

### 方式二：使用批处理脚本

双击 `安装依赖.bat`（需要先创建这个文件）

## 第三步：启动程序

### 1. 启动信令服务器

**方式一：使用批处理**
- 双击 `start-server.bat`

**方式二：手动启动**
```
cd server
npm start
```

看到以下信息表示成功：
```
YCDesk 信令服务器运行在端口 3000
WebSocket 服务器已启动
```

### 2. 启动客户端

**方式一：使用批处理**
- 双击 `start-client.bat`

**方式二：手动启动**
```
npm start
```

## 常见问题

### 问题 1：npm install 下载慢

已配置 `.npmrc` 使用国内镜像源，如果还是慢，可以：
```
npm config set registry https://registry.npmmirror.com
npm config set electron_mirror https://npmmirror.com/mirrors/electron/
```

### 问题 2：Electron 安装失败

删除 node_modules 重新安装：
```
Remove-Item -Recurse -Force node_modules
npm install
```

### 问题 3：端口 3000 被占用

修改 `server/server.js` 中的 PORT 常量，或关闭占用端口的程序。

### 问题 4：屏幕预览没有权限

在 Windows 上，确保以管理员身份运行，或在设置中允许屏幕录制权限。

## 功能说明

### 已完成功能
- ✅ 设备 ID 生成和显示
- ✅ 点击复制设备 ID
- ✅ 本地屏幕预览
- ✅ 远程控制窗口框架

### 开发中功能
- 🔄 WebRTC 远程连接
- 🔄 远程屏幕共享
- 🔄 鼠标键盘控制

## 调试模式

如果需要调试，可以使用：
```
npm run dev
```

这会自动打开开发者工具。

---

**祝你使用愉快！** 🎉
