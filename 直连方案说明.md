# YCDesk 直连方案说明

## 一、信令服务器部署位置

### 方案1：公网云服务器（推荐生产环境）
**适用场景：** 需要对外部用户提供服务
- **位置：** 阿里云、腾讯云、AWS等云服务器
- **优点：** 
  - 24小时在线
  - 所有用户都能连接
  - 稳定可靠
- **缺点：** 需要付费购买云服务器
- **配置：** 
  - 需要开放端口：3000
  - 域名（可选）

### 方案2：局域网内某台电脑（适合内部使用）
**适用场景：** 公司或家庭局域网内使用
- **位置：** 局域网内一台常开的电脑
- **优点：**
  - 免费
  - 延迟低
  - 安全（不对外暴露）
- **缺点：**
  - 只能在同一局域网内使用
  - 这台电脑必须一直开着

### 方案3：其中一台客户端电脑上（临时测试）
**适用场景：** 临时测试、两台电脑直连
- **位置：** 在作为被控端的电脑上运行
- **优点：**
  - 无需额外设备
  - 快速测试
- **缺点：**
  - 这台电脑必须一直开着
  - 主控端需要知道这台电脑的IP

---

## 二、两台电脑都有固定外网IP的直连方案

### 前提条件
✅ 两台电脑都有**固定的公网IP地址**（IPv4或IPv6）
✅ 两台电脑的防火墙都开放了WebRTC需要的端口
✅ 不需要NAT穿透（因为有公网IP）

### 方案A：简化信令 + 公网IP直连（推荐）

**仍然使用轻量信令服务器，但配置为直连模式：**

1. **在两台电脑都能访问的地方运行信令服务器**（比如其中一台有公网IP的电脑上）

2. **修改 WebRTC 配置，不使用 STUN/TURN**（因为有公网IP）

修改 `index.html` 中的 ICE_CONFIG：

```javascript
const ICE_CONFIG = {
  iceServers: []  // 空数组，不使用STUN/TURN服务器
};
```

3. **优点：**
   - 仍然使用信令服务器交换连接参数（简单可靠）
   - 实际媒体流直接在两台电脑间传输
   - 配置简单，不需要手动复制粘贴参数

### 方案B：完全无信令服务器（手动交换参数）

如果完全不想用信令服务器，可以：

1. **主控端（电脑A）：**
   - 生成 WebRTC Offer
   - 将 Offer 复制发送给电脑B（通过QQ、邮件等）

2. **被控端（电脑B）：**
   - 接收 Offer
   - 生成 WebRTC Answer
   - 将 Answer 发回给电脑A

3. **电脑A：**
   - 接收 Answer
   - 建立连接

**缺点：** 需要手动复制粘贴，用户体验不好

---

## 三、IPv4 和 IPv6 配置

### IPv4 直连配置

```javascript
// 示例：对方IPv4地址为 203.0.113.10
const ICE_CONFIG = {
  iceServers: [],
  iceTransportPolicy: 'all'
};

// 在生成候选时，优先使用公网IPv4
peerConnection = new RTCPeerConnection({
  ...ICE_CONFIG,
  iceCandidatePoolSize: 10
});
```

### IPv6 直连配置

```javascript
// 示例：对方IPv6地址为 [2001:db8::1]
const ICE_CONFIG = {
  iceServers: [],
  iceTransportPolicy: 'all'
};

// IPv6地址格式注意用方括号括起来
// 例如：ws://[2001:db8::1]:3000
```

### 同时支持 IPv4 和 IPv6

```javascript
const ICE_CONFIG = {
  iceServers: [],
  iceTransportPolicy: 'all'
};

// WebRTC会自动选择最快的连接方式
// 优先IPv6，降级到IPv4
```

---

## 四、防火墙配置

### 需要开放的端口（重要！）

即使有公网IP，也需要配置防火墙：

**Windows 防火墙入站规则：**
- UDP 端口范围：1024-65535（WebRTC媒体流）
- TCP 端口 3000（信令服务器，如果用的话）

**路由器端口映射（如果在NAT后）：**
- 将上述端口映射到电脑的内网IP

---

## 五、推荐方案总结

### 如果有两台有固定公网IP的电脑：

**最佳方案：方案A（简化信令 + 直连）**

1. 在其中一台电脑上运行简化版信令服务器
2. 配置 WebRTC 不使用 STUN/TURN
3. 两台电脑通过信令服务器交换参数
4. 实际媒体流直接点对点传输

**优点：**
- 配置相对简单
- 用户体验好（不需要手动复制）
- 延迟最低（直连）
- 节省信令服务器带宽（媒体流不走服务器）

---

## 六、快速测试步骤

1. **确保两台电脑都有公网固定IP**
2. **配置防火墙开放端口**
3. **在电脑B上运行信令服务器**（`cd server && node server.js`）
4. **电脑A和电脑B都运行YCDesk客户端**
5. **修改 `index.html` 中的 `ICE_CONFIG` 为空数组**
6. **电脑A输入电脑B的设备ID并连接**
7. **完成！**

媒体流将直接在两台电脑间传输！
