# 鼠标坐标计算方式详解

## 坐标计算流程

### 1. 触摸坐标获取
- 来源：屏幕上的触摸点 `clientX, clientY`
- 这是相对于浏览器视口（viewport）的坐标

### 2. 坐标系转换

```
屏幕触摸坐标 (clientX, clientY)
        ↓
[减去 videoContainer 的偏移量]
        ↓
容器内坐标 (containerX, containerY)
        ↓
[通过 MatrixTransformer 转换]
        ↓
远程桌面坐标 (remoteX, remoteY)
        ↓
[归一化到 0-1 范围]
        ↓
发送到被控端 (normalizedX, normalizedY)
```

## 详细步骤

### 步骤1：获取 videoContainer 位置
```javascript
const videoContainerRect = videoContainer.getBoundingClientRect();
// videoContainerRect.left: videoContainer 左侧距离屏幕左边缘的距离
// videoContainerRect.top: videoContainer 顶部距离屏幕顶部的距离
```

### 步骤2：转换为容器内坐标
```javascript
const containerX = clientX - videoContainerRect.left;
const containerY = clientY - videoContainerRect.top;
```

### 步骤3：MatrixTransformer.containerToRemote() 计算
```javascript
// 先转换为 display 坐标（考虑缩放和平移）
const displayX = containerX - this.displayX;
const displayY = containerY - this.displayY;

// 应用逆变换（考虑缩放和平移）
const centerX = this.displayWidth / 2;
const centerY = this.displayHeight / 2;

const transformedX = centerX + (displayX - centerX - this.panX) / this.scale;
const transformedY = centerY + (displayY - centerY - this.panY) / this.scale;

// 转换为远程桌面坐标
const remoteX = (transformedX / this.displayWidth) * this.remoteScreenWidth;
const remoteY = (transformedY / this.displayHeight) * this.remoteScreenHeight;
```

### 步骤4：归一化坐标
```javascript
const normalizedX = remoteX / this.remoteScreenWidth;  // 0-1
const normalizedY = remoteY / this.remoteScreenHeight; // 0-1
```

### 步骤5：被控端转换回像素坐标
```javascript
// 被控端 input-handler.js
const pixelX = Math.round(normalizedX * screenWidth);
const pixelY = Math.round(normalizedY * screenHeight);
```

## 关键变量说明

| 变量 | 说明 |
|------|------|
| `clientX, clientY` | 触摸点在屏幕上的坐标（相对于视口） |
| `videoContainerRect` | videoContainer 的位置信息（left, top, width, height） |
| `containerX, containerY` | 触摸点在 videoContainer 内的坐标 |
| `displayWidth, displayHeight` | 计算出的视频显示区域尺寸（保持宽高比） |
| `displayX, displayY` | 视频显示区域相对于 videoContainer 的偏移 |
| `remoteScreenWidth, remoteScreenHeight` | 远程桌面的实际分辨率 |
| `panX, panY` | 用户平移操作的偏移量 |
| `scale` | 用户缩放操作的缩放比例 |

## 常见问题排查

### 鼠标位置偏下/偏左
可能原因：
1. videoContainer 的位置计算不正确
2. displayWidth/displayHeight 计算有误
3. 视频没有正确居中显示
4. 屏幕方向变化后没有重新计算

### 调试建议
在 `InputDispatcher.dispatchTouchInput()` 中查看日志：
```
InputDispatcher: displayWidth=..., displayHeight=...
InputDispatcher: 发送输入 - clientX=..., clientY=..., remoteX=..., remoteY=...
```
